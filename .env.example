# Copiar a .env y rellenar con tus valores

# Servidor
PORT=4000
NODE_ENV=development

# PostgreSQL Local
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=tu_contraseña_postgres
PGDATABASE=api_paraguay

# JWT (generar con: openssl rand -hex 32)
JWT_SECRET=cambiar_esto_a_una_cadena_aleatoria_segura_minimo_32_caracteres